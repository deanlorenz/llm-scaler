# Session ledger — 2026-08-30-ct3-s6

Mission: single-analyzer
Worktree: worktrees/single-analyzer (branch single-analyzer)
Started: 2026-08-30T17:58

## Log

- Session start. Resumed via /resume-mission. All prior ledgers verified, no pending sessions to clear. Feature worktree already active (workspace set to single-analyzer). Current tip: e4106109.

## CT3b work — 2026-08-30

- Confirmed optimizer-side helpers were called exclusively with single-element slices
  (`[]NamedAnalyzerResult{req.CompositeSignal}`) after CT2.
- User clarified helpers are kept (multi_backup/) for future engine-side use — not deleted.
- Identified `sortVariantsForScaleDown` in cost_aware_optimizer.go also needed backup
  (uses `Σ_i Score_i·PRC_i` loop over slice).
- `RolePairedState` collapsed from `[]map[string]float64` (analyzer-index × role) to
  `map[string]float64` (role only) — the analyzer-index dimension was purely an artifact of
  the multi-entry design.
- 7 helpers + `sortVariantsForScaleDown` + `fairShareValue` + `reclaimRole` +
  `scaleDownRoleIterated` + `allocateForModelPaired` + `RolePickFn` all updated.
- Call sites updated in greedy_score_optimizer.go, cost_aware_optimizer.go, rescale.go.
- Test file updated to match new single-entry signatures.
- Pre/post comparison: 252→249 specs, 0 failures. 3 dropped test cases (multi-entry
  roleBottleneckReplicas, needsScaleDownForRole×3, safeRemovalReplicasForRole) preserved
  in multi_backup/analyzer_helpers_multi_test.go with //go:build ignore.
- Committed as b980f682 on single-analyzer.
- CT3b is DONE. CT3a (the written contract doc) is still pending — user may want to
  skip or fold into CT3b completion notes given the work landed cleanly without it.

## CT5 — 2026-08-30

- Doc comment added to `initRoleState` covering: roles from RoleCapacities keys only;
  estimateSchedulerQueueDemand covers ordinary zero-replica cold-start; remaining gap is
  no-queue-signal + discovery-side omission; mixed-P/D+both deferral noted.
- Errata note added to scale-from-zero-and-fallback-trace-2026-08-25.md correcting the
  headline conclusion that "no fallback exists."
- Committed fcf9c905 (single-analyzer), 997c3220 (session-tracking errata).
- STATE.md and spec both updated to DONE.
- All unblocked tasks now complete. CT4 remains blocked on user decision.

## Engine-side reduce planning — 2026-08-30

Starting to plan the real multi-analyzer compose logic for composeAnalyzerResults.
User's invariant: never produce a result with less coverage than sat alone.

Key structural findings:
- composeAnalyzerResults works at rawAnalyzerResult level (before buildNamedResult).
  Each rawAnalyzerResult carries: name, *domain.AnalyzerResult, scaleUp, scaleDown.
- domain.AnalyzerResult carries: TotalDemand, RoleDemand, VariantCapacities (PRC, ReplicaCount, etc.).
- Units differ across analyzers — cannot add/average TotalDemand directly.
- Saturation always owns PRC; the composite must keep sat's PRC for all variants.
- Correct reduce: for each variant, take max(implied_replicas_i) across informative
  analyzers, back-convert to sat's demand units. Always >= sat's own demand.

Open questions raised, not yet resolved:
1. TotalDemand consistency — model-level vs per-variant recompute.
2. RoleDemand for disaggregated models when non-sat analyzer doesn't emit it.
3. Name of composite — "composite" vs SaturationAnalyzerName (one downstream check depends on it).
4. Score for the composite entry.

User: proceed slowly, no rush to implement. Discussion first.

## Session continuation — single-analyzer6 (2026-08-31)

### Doc fixes — optimizer-call-stack-2026-08-30.md
- Fixed several indentation/structural errors in the call-stack tree:
  - `detectDemandLiveness` note: removed spurious `|` continuation, used plain indent
  - `return &ModelScalingRequest{` nested correctly under `collectV2ModelRequest`
  - `fairShareScaleUp` aligned as sibling of `classification pass`
  - `nr := req.AnalyzerResults[0]` and `s := []NamedAnalyzerResult{...}` collapsed
    into a correct two-minus-one-plus diff (both old lines → one new line)
  - Confirmed `nr` still lives inside `recordsForRequest` (not removed); doc corrected
- Fixed engine-call-stack-2026-08-30.md: removed stale reference to
  compose-reduce-design-2026-08-30.md; fixed nested indentation on two nodes.

### Engine revert — 2026-08-31
- User identified that CT2's engine approach was wrong:
  - T1 (`composeAnalyzerResults` stub, `rawAnalyzerResult` type) was unnecessary patch-over-patch.
  - The correct design: engine works unchanged vs main (`runAnalyzersAndScore` returns
    `[]NamedAnalyzerResult`); `collectV2ModelRequest` just passes `namedResults[0]` as
    `CompositeSignal` — sat is always first by construction.
  - Optimizer side: `saturationNamedEntry(req.AnalyzerResults)` calls replaced by
    direct `req.CompositeSignal` reads (CT2 work, already done).
- Reverted engine_v2.go: removed `composeAnalyzerResults`, `rawAnalyzerResult`,
  the intermediate `namedResults := []NamedAnalyzerResult{namedResult}` wrapper,
  the single-element slice hacks around liveness/metrics.
- Added `namedResults[0]` as `CompositeSignal` in `collectV2ModelRequest`; initially
  as a picker loop (searching by name), then simplified to `namedResults[0]` directly
  after user correctly pointed out sat is always first by construction.
- Removed `engine_v2_compose_test.go` (tested the now-deleted `composeAnalyzerResults`);
  preserved in `multi_backup/engine_v2_compose_test.go` with `//go:build ignore`.
- Fixed all test files that called `runAnalyzersAndScore` expecting a scalar return
  (liveness tests, population tests, nil-guard test, external registry test).
- User: nil guard (CT1b) does not belong in this PR — removed from engine_v2.go and
  its test from engine_v2_test.go.
- Tests: all pass, 3 skips (demand liveness, same as before).
- Updated engine-call-stack-2026-08-30.md to be a clean diff-vs-main (not diff-vs-reverted-code).

### PR branch — pr/single-analyzer
- Created worktree at `/home/dean/code/llm-d/worktrees/pr-single-analyzer`
  on branch `pr/single-analyzer` off `origin/main`.
- Two clean commits:
  1. `7c3f2cde` — fix(allocation): nil-guard rescaleModelDecisions (CT1a)
  2. `38f55694` — refactor(allocation): replace AnalyzerResults with CompositeSignal
     (CT2 + CT3b + CT5 combined; includes multi_backup/)
- No nil guard (CT1b) in PR branch — excluded per user instruction.
- docs/plans/analyzers/README.md excluded from PR (session-internal housekeeping note,
  no value upstream); removed via amend after initial commit.
- All tests pass on PR branch; 3 skips (pre-existing demand liveness).

### Open items
- CT4 (fairShareValue semantics: absolute demand vs coverage ratio) — still blocked
  on user decision.
- 3 skipped demand liveness tests — need rewrite once multi-analyzer story is redesigned.
- `updateLivenessAndSetLive`, `detectDemandLiveness`, `recordAnalyzerMetrics` still take
  `[]NamedAnalyzerResult` — cleanup possible in follow-up PR (not blocking).
- Real `composeAnalyzerResults` reduce — future work, design discussion still pending.
- PR created: https://github.com/deanlorenz/llm-scaler/pull/1 (draft, reviewer: evgenyshabalin)
- Companion gists:
  - Engine call stack: https://gist.github.com/deanlorenz/9a7fd2db0a18b73ef2b7047083454852
  - Optimizer call stack: https://gist.github.com/deanlorenz/5167f326ab2641a45adbf05c3c7dc99a
- Gist source files saved at .session/gist_engine.md and .session/gist_optimizer.md

### Key file locations
- PR worktree: `/home/dean/code/llm-d/worktrees/pr-single-analyzer` (branch `pr/single-analyzer`)
  registered in git as: `git worktree add /home/dean/code/llm-d/worktrees/pr-single-analyzer origin/main -b pr/single-analyzer`
- Feature worktree: `worktrees/single-analyzer` (branch `single-analyzer`) — still has
  the full history including reverted T1 commits; used as source of truth for diffs.
- Plan docs (on single-analyzer branch): `docs/plans/analyzers/engine-call-stack-2026-08-30.md`,
  `docs/plans/analyzers/optimizer-call-stack-2026-08-30.md` — both finalized, diff-vs-main.
- multi_backup files: `internal/engines/allocation/multi_backup/` — all `//go:build ignore`.

## Verified 2026-08-31 — folded in: CT2 Status in spec updated from "NOT STARTED" to DONE with commit refs and engine-revert note; spec version bumped v5→v6. All other ledger points already captured in STATE.md (engine revert, PR branch, open items, CT1b exclusion, 3 skipped tests, `updateLivenessAndSetLive`/`detectDemandLiveness`/`recordAnalyzerMetrics` cleanup deferred).

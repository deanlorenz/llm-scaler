# Ledger: 2026-09-03 — agentbus3 (Bob/VSCode)

**Session:** agentbus3
**Date:** 2026-09-03
**Status:** retired

## Summary of Work Done

1. **TTY fix (`agentbus-dialogue`)** — `openTTY()` opens `/dev/tty` for scanner input so process persists when stdin is piped in VS Code terminal. Commit `401d31fc`.

2. **`agentbus-pub` CLI** — one-shot publish tool for shell scripting/testing. Commit `38432e62`.

3. **readline line-editing** — replaced `bufio.Scanner` with `github.com/chzyer/readline` for proper arrow key and backspace support. Commits `00aac49d`, `fbc76bef`.

4. **OPERATIONS.md** — added `agentbus_ask_user` documentation (all three modes), input behaviour, CLI quick-reference table, `--help` fix for `agentbus-setup`. Commit `f51a5dd6`.

5. **Async `agentbus_ask_user`** — receiver-based filtered subscription design and implementation:
   - `schema.Message` gains `Receiver` field
   - `bus.Subscriptions` gains `Filters map[string]TopicFilter`; new `SubscribeFiltered`, `RemoveFilter`
   - `bus.FetchBySeq` for re-ask original body lookup
   - `agentbus-dialogue` sets `Receiver=msg.From.Session` on all replies
   - `agentbusd/tools`: async mode (returns seq immediately), re-ask/wait mode (`previous_seq`), sync unchanged
   - `agentbus-hook`: applies receiver filter, cursor not advanced on fetch error
   - `agentbus-relay`: updated to new `ReadSubs` return type
   - DESIGN.md updated with full async design
   - Commits `2f5ba9d4`, `2e089132`

6. **Code review + fixes** — subagent review; fixed: hook cursor-on-error, `writeCursor` error logging, `waitForReply` cancel vs timeout distinction, `RemoveFilter` cleanup in re-ask, dialogue subscribe error handling, indentation bug. DESIGN.md synced. Commits `29aa6885`, `f6a03059`.

## Test Results

- Sync round-trip: PASS
- Async ask (returns immediately, writes receiver filter): PASS
- Re-ask/previous_seq: PASS (MCP mechanics correct)
- Dialogue async UX: BROKEN — sequential blocking loop; new messages cannot be shown while awaiting reply. Needs concurrent input/display model (bubbletea TUI or goroutine split). Stopping here.

## Artifacts & Commits

- `401d31fc` TTY fix
- `38432e62` agentbus-pub
- `00aac49d` readline
- `fbc76bef` multi-line continuation
- `f51a5dd6` OPERATIONS.md
- `2f5ba9d4` DESIGN.md async design
- `2e089132` async ask_user implementation
- `29aa6885` code review fixes
- `f6a03059` DESIGN.md sync

# Session ledger — 2026-09-01-s8

Mission: single-analyzer
Worktree: worktrees/single-analyzer (branch single-analyzer)
Started: 2026-09-01

## Log

- Session start. Resumed via /resume-mission. Prior session (2026-08-31-s7) confirmed retired
  and already carries ## Verified marker — no ledger-capture needed.
- Checked PR #34 status: still open, draft, `total_count: 0` on status API (GitHub Actions uses
  Checks API, not status API — same caveat as s7). PR open, no new reviews.
- Ran `go test ./internal/engines/...` — allocation package build failed. Root cause: s7's lint
  fix removed the name/score params from named(), withScore(), and makeNamed() but missed 8
  stale call sites across two test files:
    - analyzer_helpers_test.go lines 41, 49, 55, 129, 169, 212: makeNamed("sat", rc, sc, ...)
      → makeNamed(rc, sc, ...)
    - greedy_score_optimizer_test.go lines 834, 844, 898, 908, 1294, 1339: withScore(1.0)
      → withScore(); named("saturation") → named()
  Note: pr-single-analyzer branch already had the correct zero-arg form — these stale callers
  existed only on single-analyzer, diverged after the s7 partial-apply.
- Fixed all 8 call sites; go test ./internal/engines/... all pass.
- Committed: b067642a "fix(allocation): remove stale named/withScore/makeNamed arguments left by s7 lint fix"
- Updated STATE.md to record session start and add this fix to the session log.
- Checked PR #34 CI via `gh pr view` (Checks API, not status API): all checks green —
  lint-and-test, e2e-tests-smoke, e2e-tests-full, DCO, signed-commits all SUCCESS.
- PR was in draft — s7 created it draft, no reason recorded; CI had been green since
  2026-08-31T18:45. Marked ready for review: `gh pr ready 34`.
- CT6 semantic framework discussion. Key outcomes:
  - Wrote formal semantic framework (Entities, D/PRC/N_full/C/NG definitions,
    multi-analyzer reduce, model-level coverage, GPU demand) into spec.md.wip CT6 section.
  - Rescale fairness: intended weight = N_full (replicas, not tokens, not GPUs).
    Current `Demand = TotalDemand` (tokens) is proportional to N_full only for homogeneous
    PRC — works today, breaks after CT6 (TotalDemand=1.0 for all models).
  - Immediate fix for CT6: add `SatDemand float64` to `NamedAnalyzerResult`, set it to
    raw `TotalDemand` before normalization, use it in `rescaleInputsForGroup` instead of
    `Result.TotalDemand`. Numerically identical to today.
  - Longer-term fix (separate CT, not CT6): replace weight with true replica-demand
    `N_full(M) = modelDemandGPUs / G̅`, GPU-independent. Tracked in spec.
  - rescaleInput.Demand open decision now RESOLVED. CT6 is unblocked.
  - Spec updated to v8 (semantic framework section + rescale fairness section +
    Todo updated with SatDemand steps).
